import { NoLeftSpaceDirective } from './no-left-space.directive';

describe('NoLeftSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoLeftSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
